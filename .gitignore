# Raw .fit files -- keep these local/in Drive, not in git (binary, large volume)
data/raw/*.fit
*.fit

# Secrets
.env
*.env
config.json

# Medical / health data -- never commit, local-only by design (see CLAUDE.md's
# "Medical data handling" section). Whole tree ignored, no committed subfolder.
data/medical/

# Throwaway diagnostic output (personal activity data, not a pipeline artifact)
ingestion/strava/_id_verification/

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
